# Interview Studio — 产品需求文档

> 状态：草案 v0.1 · 2026-08-25
> 目标：把现有的一堆 bash 脚本整理成一个能装、能分发、不会在关键时刻掉链子的桌面应用。

---

## 1. 为什么做这个

现有的 `interview-rec.sh` 功能是对的，但有三个结构性问题：

**其一，看不见状态。** 录了 39 分钟，事后才知道两条轨录得怎么样。实际发生过：用户以为面试官的声音没录到，慌了半天，最后发现是录到了、只是播放器默认只播第一条轨。整个惊吓的根源是**过程中没有任何可见反馈**。

**其二，路由错位是静默失败。** 工具录的是「默认输出设备」的 monitor，但 PipeWire 会按应用记住输出设备（实测该机器上已有 2 条 `module-stream-restore` 记录）。完全可能出现：默认设备是耳机、腾讯会议被记成了 HDMI —— 录出来对方那轨全程静音，而你耳朵里一切正常。现有工具只有手动 `--test` 能发现，而且要记得跑。

**其三，分发不了。** bash + zenity + pactl 绑死 Linux，换台机器要手动摆脱本、装依赖、配快捷方式。

---

## 2. 用户与场景

**主用户**：求职者本人（单人使用，非团队协作）。

**核心场景**：
1. 面试前 5 分钟打开 app，点「开始前检查」，确认两路都能录到
2. 面试中最小化，偶尔瞥一眼确认还在录、两条轨都有电平
3. 面试后自动出一份带角色和时间戳的对话稿，用于复盘
4. 隔几周回头翻某场面试，按关键词搜文稿定位到音频位置

**次要场景**：技术分享、组会、线上课，任何「需要事后复盘的双向语音」。

---

## 3. 非目标

明确不做，避免范围蔓延：

- ❌ 多人协作、云端同步、账号体系
- ❌ 实时字幕（延迟和准确率都不够，会分散面试注意力）
- ❌ 视频录制（面试复盘看不看画面价值很低，体积代价极大）
- ❌ 说话人分离算法（双轨天然分离，不需要 diarization）
- ❌ LaTeX 预览（`texv` 保持独立项目，与本应用无关）
- ❌ 会议平台 API 集成（monitor/loopback 是平台无关的，不需要也不该依赖具体软件）

---

## 4. 功能需求

### P0 — 第一版必须有

| # | 功能 | 验收标准 |
|---|---|---|
| F1 | **开始前检查** | 一个按钮走完：设备解析 → 路由检查 → 10 秒试录 → 两轨电平判定。任一项不过给出**可执行的**修复建议（不是「失败了」而是「腾讯会议在用 HDMI，点这里改用耳机」） |
| F2 | **双轨录音** | 麦克风与系统输出分两条轨；进程异常退出时文件仍可播放（写完容器尾） |
| F3 | **实时双轨电平表** | 录音中持续显示两条轨的电平；任一轨连续 30 秒低于 −80 dB 时醒目告警。这条直接消灭第 1 节说的「其一」和「其二」 |
| F4 | **自动混音轨** | 录完生成第三条轨（双方混音）并设为默认轨，双击播放即可听到两人。混音必须过限幅器 —— 实测直接相加有 486 个采样撞满刻度 |
| F5 | **原轨无损** | 混音过程中原两轨逐比特不变（以解码后哈希验证） |
| F6 | **分轨转写** | 两轨分别送 ASR，按时间归并成带角色的对话稿 |
| F7 | **录音库** | 列出历史录音：时长、日期、两轨电平、是否已转写 |

### P1 — 第二版

| # | 功能 | 说明 |
|---|---|---|
| F8 | 文稿检索 | 全文搜索，点结果跳到音频对应位置 |
| F9 | 导出 | Markdown / 纯文本 / SRT |
| F10 | 术语表 | 复用 `~/.config/qwen-asr/context.txt` 的上下文注入，纠正专业术语 |
| F11 | 录音中标记 | 快捷键打点，事后直接跳到「这里答得不好」 |
| F12 | 设备热插拔应对 | 录音中默认设备变化时告警（拔耳机会让 monitor 断流） |

### P2 — 以后再说

- 摘要/复盘（把文稿喂给本地 LLM 出「被问到的知识点 / 答得不好的地方」）
- Windows 版完整实现
- 多轨（会议有多个音频源时）

---

## 5. 界面设计

比现在的 zenity 弹窗丰富得多，但仍然克制 —— 面试前打开它的人是紧张的，界面要一眼看懂。

```
┌──────────┬──────────────────────────────────────────────────┐
│          │                                                  │
│  ● 录音  │   ┌── 开始前检查 ─────────────────────────────┐  │
│          │   │  ✓ 麦克风    内置麦克风             −32dB │  │
│  ▤ 文稿  │   │  ✓ 系统输出  USB 耳机               −24dB │  │
│          │   │  ✓ 路由     会议软件走的就是默认输出       │  │
│  ▦ 库    │   │                            [重新检查]     │  │
│          │   └───────────────────────────────────────────┘  │
│  ⚙ 设置  │                                                  │
│          │   我(麦克风)   ▂▄▆█▆▄▂▁▂▄▆  −28 dB               │
│          │   对方(系统)   ▄▆█▇▅▃▂▄▆█▇  −22 dB               │
│          │                                                  │
│          │              ⏺  开始录音                          │
│          │                                                  │
│          │        00:12:34   ·   142 MB   ·   剩余 38 GB     │
└──────────┴──────────────────────────────────────────────────┘
```

**四个页面：**

- **录音** — 检查卡片 + 双轨实时电平 + 大按钮 + 时长/体积/剩余空间
- **文稿** — 左侧时间轴，右侧对话稿；点任一句跳播到该处；角色用颜色区分
- **库** — 历史录音卡片列表，显示两轨电平摘要（一眼看出哪场录砸了）
- **设置** — 设备选择、ASR 端点、存储路径、音质、自动转写开关

**设计原则：**

1. **电平表是主角，不是装饰。** 第 1 节那三个问题全都源于「看不见」。两条会动的波形放在最显眼处，比任何弹窗都有效。
2. **失败要给出下一步。** 不说「路由错误」，说「腾讯会议在用 HDMI，[改用耳机]」并且那是个能点的按钮。
3. **录音中界面必须极简。** 面试时不能有任何需要思考的东西，只留时长、两条电平、停止按钮。

---

## 6. 技术架构

```
crates/
├── is-audio/      设备枚举、路由检查、录音后端 trait   [已完成骨架]
├── is-pipeline/   混音+限幅、静音切片、ASR 客户端
├── is-store/      录音库元数据（SQLite 或 JSON 边车文件）
└── is-app/        egui 界面
```

### 选型与理由

| 层 | 选型 | 理由 |
|---|---|---|
| GUI | egui / eframe 0.35 | 单静态二进制、无运行时依赖；已有 `texv` 作为跑通的样板 |
| 音频枚举 | Linux: `pactl` | cpal 在 Linux 默认走 ALSA，看不到 monitor 源。`pactl` 这条路已被现有脚本几十场录音验证 |
| 音频枚举 | Windows: `cpal` | WASAPI 后端已有 `AUDCLNT_STREAMFLAGS_LOOPBACK`（已在源码确认） |
| 录制/编码 | ffmpeg 子进程 | 第一版保留。它替我们处理了双输入的时钟对齐 |
| 转写 | 本机 `llama-server` + Qwen3-ASR | 已在跑，实测 **16 倍实时**，39 分钟约 2 分钟 |

### 为什么第一版保留 ffmpeg

自己实现 FLAC 编码（`flacenc`）、混音、限幅是可行的，但 ffmpeg 现在替我们处理了一件不显眼却关键的事：**两路输入的时钟对齐**。麦克风和 monitor 是两个独立时钟域，40 分钟下来会漂移。等到做 Windows 版必须直面这个问题时再一起解决，不要在第一版就自找。

代价：Linux 上声明成包依赖，Windows 打包时随包带一份（LGPL 动态链接可以）。

### Windows 留的接口

`is-audio::Backend` trait 就是分界线。Windows 实现同一套接口，上层不改。真正的工作量不在拿到 API，而在：

- 麦克风流与 loopback 流是两个时钟域，需按时间戳重采样对齐（`rubato`）
- loopback 在没有任何声音播放时不产生数据，静音段要自己补

---

## 7. 打包

| 目标 | 工具 | 备注 |
|---|---|---|
| `.rpm` | `cargo-generate-rpm` 0.21 | 依赖声明 `ffmpeg`、`pipewire-pulseaudio` |
| `.deb` | `cargo-deb` 3.7 | 同上 |
| Windows | `cargo-wix`（MSI）或便携 zip | 需随包带 ffmpeg |
| 桌面集成 | `.desktop` + 图标 | 保留右键「开始前检查」动作 |

不用 `cargo-packager`（一把梭三平台）的原因：它最后更新是 2025-11，而三个专用工具都在活跃维护。

---

## 8. 里程碑

| | 内容 | 产出 |
|---|---|---|
| **M0** | `is-audio`：设备枚举 + 路由检查 | ✅ 已完成，9 个测试通过（5 个跑真实硬件） |
| **M1** | `is-pipeline`：录音、混音+限幅、无损校验 | 命令行可完整走通一次录音 |
| **M2** | `is-app`：录音页 + 开始前检查 + 实时电平表 | **可以拿去面试用了** |
| **M3** | 转写与文稿页 | 双轨归并出对话稿 |
| **M4** | 库页、设置页、导出 | 功能完整 |
| **M5** | rpm / deb 打包 | 可分发 |
| **M6** | Windows 后端 | 三平台 |

M2 是第一个有实用价值的版本，应该优先冲到那里。

---

## 9. 风险与未解问题

| 风险 | 影响 | 应对 |
|---|---|---|
| 录音中拔耳机导致 monitor 断流 | 后半段静音 | F12：监测默认设备变化并告警。**根治需要中途切换录音源，代价大，第一版只告警** |
| ffmpeg 子进程异常退出 | 录音中断且可能无感知 | 心跳监测 + 显著告警；文件已写部分保持可播 |
| ASR 上下文 8192 限制 | 长录音需切片 | 按静音点切片而非固定时长，避免拦腰截断句子 |
| 磁盘写满 | 录音中断 | 录前检查（已有）+ 录中持续显示剩余空间 |
| 隐私 | 录音含第三方声音 | 全程本地处理，不上传任何云服务。合规性由使用者判断 |

**未解问题**（需要决定）：

1. 录音库元数据用 SQLite 还是边车 JSON？SQLite 利于 F8 全文搜索，但多一个依赖
2. 转写是录完自动跑，还是手动触发？自动更省事，但占用资源且不是每场都需要
3. 应用名与二进制名 —— 目前暂用 `interview-studio`，太长

---

## 10. 验收标准

第一版（M2）达标的判据：

- [ ] 冷启动到「开始录音」可点击 < 2 秒
- [ ] 开始前检查全流程 < 15 秒，且能正确识别路由错位（构造：把会议软件手动指到非默认设备）
- [ ] 连续录音 60 分钟不崩、不丢帧、内存不增长
- [ ] 强杀进程后文件仍可播放，且两轨完整
- [ ] 混音轨无削顶（`max_volume` ≤ −1.0 dB）
- [ ] 原两轨解码后哈希与录制结果逐比特一致
